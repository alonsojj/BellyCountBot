[project]
name = "bellycountbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi[standard]>=0.119.1",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.1",
]
